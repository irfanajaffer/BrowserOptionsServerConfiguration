@namespace BlazorServerApp.Components

<h1>StaticServer.PreserveDom demo</h1>
<p>Choose which root to load:</p>
<ul>
    <li><a href="/preserve">PreserveDom = true</a></li>
    <li><a href="/replace">PreserveDom = false</a></li>
</ul>

<p>Open the browser devtools and interact with the demo (type in the input, set focus) to observe differences when the interactive render hydrates the server-rendered DOM.</p>
